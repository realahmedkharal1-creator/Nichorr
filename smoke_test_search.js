const { WebSearchProvider } = require('./src/lib/search/web.search.provider.ts'); (async () => { const provider = new WebSearchProvider(); try { await provider.search('test', 'PRIMARY', false); console.log('FAIL: Should have thrown configuration error'); } catch (e) { if (e.message.includes('TAVILY_API_KEY is not configured')) { console.log('PASS: Search honestly fails when missing configuration.'); } else { console.log('FAIL: Unexpected error: ' + e.message); } } })();
