---
description: Rule enforcing strict accounting of new and modified files before any git commit.
---

# Commit Reporting Rule

Before every commit, you **MUST** run `git status`.
You must explicitly account for **every single** new or modified file in your final report to the user. No file gets committed without being named in your report and having a valid reason provided.
