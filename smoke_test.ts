import { WebSearchProvider } from './src/lib/search/web.search.provider'; (async () => { const provider = new WebSearchProvider(); try { await provider.search('test topic', 'PRIMARY', false); console.log('FAIL: Did not throw configuration error.'); process.exit(1); } catch (e: any) { if (e.message.includes('TAVILY_API_KEY is not configured')) { console.log('PASS: Search honestly fails when missing configuration. Error: ' + e.message); process.exit(0); } else { console.log('FAIL: Unexpected error: ' + e.message); process.exit(1); } } })();
